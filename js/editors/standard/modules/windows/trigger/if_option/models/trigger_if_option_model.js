var TriggerIfOptionModel = Backbone.Model.extend({
    defaults:{
        variable: '', // Ok
        action: '',   // OK
        conditionID: '',
        compareValue: '', // Ok
        actionType: '', // Ok
        andor: 'and'  // Ok

    }
});
